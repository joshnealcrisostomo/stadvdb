import React from 'react';
import '../css/header.module.css';

const Header = () => {
    return (
        <header className="header-title">
            <h1>Philippine Energy Mix & Weather</h1>
        </header>
    );
};

export default Header;